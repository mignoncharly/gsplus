# Phase 10 route-specific static HTML support.
# Insert `error_page` at server scope and replace the existing `location /`.

error_page 404 /404.html;

location = /404.html {
    internal;
}

location / {
    try_files $uri.html $uri $uri/ =404;
}
